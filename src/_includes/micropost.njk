<div style="margin-bottom: 10px;" data-pagefind-body>
    <h2>{{ post.title }}</h2>

    <div class="post-date">
        {{ post.date | toDateTime }}
        {% if post.tags.length > 0 %}
            {% for tag in post.tags %}
                #<a href="/tags/{{ tag | slug }}">{{ tag }}</a>
            {% endfor %}
        {% endif %}
    </div>

    {{ post.content | safe }}

    {% for ytId in (post | getYouTubeLinks) %}
        <lite-youtube videoid="{{ ytId }}"></lite-youtube>
    {% endfor %}

    {% if post.type !== 'mb' %}
        {% if post.attachments.length > 0 %}
        <div class="photogrid">
            {% for image in post.attachments %}
                {% if image.type === 'video' %}
                    <video controls>
                        <source src="{{ image.url | imageLink }}" type="video/mp4">
                    </video>
                {% else %}
                    <img src="{{ image.url | imageLink }}" alt="{{ image.description }}">
                {% endif %}
            {% endfor %}
        </div>
        {% endif %}
    {% endif %}

    <div style="font-size: 0.8em; text-align: right;margin-top: 10px;">
        {% if post.source %}
            <a target="_blank" rel="noopener" href="{{ post.source }}">{{ post.source }}</a>
        {% endif %}
        <a href="{{ post.path }}">&infin;</a>
    </div>
</div>
