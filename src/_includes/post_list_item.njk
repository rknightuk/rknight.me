

<div class="entryitem">
    <div class="entryitem--data">
        <span>{% if post.data.featured %}⭐{% endif %} <a href="{{ post.data.permalink | stripIndex }}">{{ post.data.title }}</a></span>
        <hr>
        {% if post.data.postType != 'note' %}
        <span class="entryitem--date">{{ post.data.date | postDateNoYear }}</span>
        {% endif %}
    </div>
    {% if post.data.excerpt and post.data.postType != 'note' %}
        <div class="entryitem--excerpt">
            <p><em>{{ post.data.excerpt }}</em></p>
        </div>
    {% endif %}
    
</div>