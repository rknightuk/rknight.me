<div class="container container-m">
    <h2>Ahoy there, I'm Robb</h2>

    <p>I'm a developer at my day job, a developer and podcaster at <a href="https://macstories.net/about">MacStories</a>, a member of <a href="http://devsdodesign.com">#DevsDoDesign</a>, an award winning hackathon team, and a podcaster with <a href="https://ruminatepodcast.com">two</a> <a href="https://wegot.family">shows</a>. I also <a href="/projects">make a bunch of stuff</a> including <a href="https://echofeed.app">EchoFeed</a>.</p>

    <p>You can find me on Mastodon as <a href="https://social.lol/@robb">robb@social.lol</a> and more things as <code>rknightuk</code>.</p>

    <p>Find out <a href="/about">about me</a> or <a href="/about/colophon">this internet website</a>.</p>

    <h2>My Writing</h2>

    <div style="display: flex; justify-content: space-between;grid-gap: 10px;">
        <div>
            {%- for post in collections.firstPosts %}
                <div style="display: grid;grid-template-columns: 1fr 1fr;">
                    <div><a href="{{ post.url }}">{{ post.data.title }}</a></div>
                    <div style="text-align: right;">{{ post.data.date.toISOString() | isoDateOnly }}</div>
                </div>
            {% endfor %}
        </div>
        <div>
            {%- for post in collections.firstPosts %}
                <div style="display: grid;grid-template-columns: 1fr 1fr;">
                    <div><a href="{{ post.url }}">{{ post.data.title }}</a></div>
                    <div style="text-align: right;">{{ post.data.date.toISOString() | isoDateOnly }}</div>
                </div>
            {% endfor %}
        </div>
    </div>


</div>