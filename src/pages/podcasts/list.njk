---
title: Podcasts
layout: base
permalink: 'podcasts/index.html'
eleventyNavigation:
  key: PodcastsList
  title: Podcasts
  parent: Home
---

I currently have two podcasts: <a href='https://ruminatepodcast.com'>Ruminate</a> and <a href='https://wegot.family'>We Got Family</a>. This is a combined feed of both shows.

{% set imagePrefix = '/assets/projects/' %}

<div class="cards" style="grid-template-columns: repeat(2,1fr);margin-top: 20px;">
{%- for project in site.projects.podcasts %}<a href="{{ project.link }}"><img style="border-radius: 10px;" src="{{ imagePrefix }}{{ project.image }}"></a>{% endfor%}
</div>

{% set datePrinted = false %}
{% set currentYear = 1000 %}

{%- for pod in mypodcasts.all %}
    {% set podYear = pod.year %}

    {% if podYear != currentYear %}
        <h3>{{ podYear }}</h3>
    {% endif %}

    <div class="entryitem">
        <div class="entryitem--data">
            <a href="{{ pod.link }}">{{ pod.show.title }} {{ pod.title | safe }}</a>
            <hr>
            <span class="entryitem--date">{{ pod.date }}</span>
        </div>
        <div class="entryitem--excerpt">
            <p><em>{{ pod.desc }}</em></p>
        </div>
    </div>

    {% set currentYear = podYear %}
{% endfor %}