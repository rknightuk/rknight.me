---
title: Lego Collection
permalink: 'collections/lego/index.html'
layout: collections
---

<style>
    .collection-image-wrapper img {
        box-shadow: none;
    }

    .collection-grid {
        grid-template-columns: 1fr 1fr 1fr 1fr;
    }
</style>

<h3>{{ lego.count.sets }} sets with {{ lego.count.pieces }} pieces across {{ lego.count.themes }} themes</h3>

<details class="collection-grid-container">
    <summary>
        <span>
            <span class="summary-wrap">
                Loose Minifigures ({{ lego.minifigures.loose.length }})
            </span>
        </span>
    </summary>
    <div class="collection-grid">
        {%- for minifig in lego.minifigures.loose %}
            <div class="collection-item-wrapper">
                <div class="collection-image-wrapper">
                    <a target="_blank" rel="noopener" href="https://brickset.com/minifigs/{{ minifig.id }}">
                        <img src="{{ minifig.image }}">
                    </a>
                </div>
                <div class="collection-label">{{ minifig.name }} ({{ minifig.id }})</div>
            </div>
        {% endfor%}
    </div>
</details>

<details class="collection-grid-container">
    <summary>
        <span>
            <span class="summary-wrap">
                Minifigures from sets ({{ lego.minifigures.sets.length }})
            </span>
        </span>
    </summary>
    <div class="collection-grid">
        {%- for minifig in lego.minifigures.sets %}
            <div class="collection-item-wrapper">
                <div class="collection-image-wrapper">
                    <a target="_blank" rel="noopener" href="https://brickset.com/minifigs/{{ minifig.id }}">
                        <img src="{{ minifig.image }}">
                    </a>
                </div>
                <div class="collection-label">{{ minifig.name }} ({{ minifig.id }})</div>
            </div>
        {% endfor%}
    </div>
</details>

{%- for theme in lego.themes %}
    <details class="collection-grid-container">
        <summary>
            <span>
                <span class="summary-wrap">
                    {{ theme }} ({{ lego.sets[theme].length }})
                </span>
            </span>
        </summary>
        <div class="collection-grid">
            {%- for set in lego.sets[theme] %}
                <div class="collection-item-wrapper">
                    <div class="collection-image-wrapper">
                        <a target="_blank" rel="noopener" href="{{ set.url }}">
                            <img src="{{ set.image }}">
                        </a>
                    </div>
                    <div class="collection-label">{{ set.title }} ({{ set.id }})</div>
                </div>
            {% endfor%}
        </div>
    </details>
{% endfor%}
