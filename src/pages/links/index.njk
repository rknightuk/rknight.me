---
pagination:
  data: collections.links
  size: 50
  alias: link
permalink: 'links/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}'
layout: base
eleventyComputed:
    title: Links
    subtitle: A collection of interesting links
---

{%- for link in pagination.items %}
    <p class="post-title"><a href="{{ link.data.link }}">{{ link.data.title }} &#x2192;</a></p>
    <p class="post-date">&infin; <a href="{{ link.data.permalink }}">{{ link.data.date | postDate }}</a></p>
    {{ link.content | safe }}
    {% if loop.last %}
    {% else %}
        <hr>
    {% endif %}
{% endfor -%}

<div class="pagination box">
    <div>{% if pagination.href.previous %}<a href="{{ pagination.href.previous }}">Newer Links</a>{% else %}{% endif %}</div>
    <div>{% if pagination.href.next %}<a href="{{ pagination.href.next }}">Older Links</a>{% else %}{% endif %}</div>
</div>
